# Reviewed benchmark evidence

This directory is the repository boundary for evidence that has completed a full real-data run and manual review. Generated output remains outside the Git worktree. Add raw CSV evidence here only deliberately, after both panel validation gates and interpretation limits have been reviewed, and commit it in a separate results-publication change.

There are currently no reviewed benchmark results in the repository.
